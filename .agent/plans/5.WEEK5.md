# UI Design file

## Week 5 Plan — Frontend UI (React + Vite + Tailwind + shadcn/ui)

**Goal:** Build a production-quality frontend for TrustRAG that connects to the existing FastAPI backend and exposes all core features through a clean enterprise UI.

---

## Deliverables

| # | Task | File(s) | Done |
|---|------|---------|------|
| 1 | Vite + React + TypeScript scaffold | `frontend/` | [ ] |
| 2 | Tailwind CSS + shadcn/ui setup | `frontend/tailwind.config.js`, `frontend/components.json` | [ ] |
| 3 | API client | `frontend/src/lib/api.ts` | [ ] |
| 4 | Shared types | `frontend/src/types/index.ts` | [ ] |
| 5 | App layout + routing | `frontend/src/App.tsx`, `frontend/src/components/Layout.tsx` | [ ] |
| 6 | Chat page | `frontend/src/pages/Chat.tsx` | [ ] |
| 7 | Ingest page | `frontend/src/pages/Ingest.tsx` | [ ] |
| 8 | Review Queue page | `frontend/src/pages/Review.tsx` | [ ] |
| 9 | TrustScore badge component | `frontend/src/components/TrustScoreBadge.tsx` | [ ] |
| 10 | README for frontend | `frontend/README.md` | [ ] |

---

## Pages

### Chat (/)
- Message thread with user questions and AI answers
- Trust score badge on each answer (green ≥ 0.75, yellow 0.5–0.74, red < 0.5)
- Source citations (document title, source type, department)
- "Needs Review" indicator when flagged
- Loading skeleton while waiting for response

### Ingest (/ingest)
- Form: document ID, title, source type (dropdown), department, text
- Submit → shows chunks created confirmation
- Add multiple documents in one session

### Review Queue (/review)
- List of pending reviews with trust score, question, answer
- Approve / Reject / Modify actions per item
- Modify opens inline editor for corrected answer

---

## Architecture

```
frontend/
├── src/
│   ├── pages/         # Chat, Ingest, Review
│   ├── components/    # Layout, TrustScoreBadge, shadcn ui/
│   ├── lib/           # api.ts, utils.ts
│   └── types/         # shared TypeScript types
```

## API Base URL

Configured via `VITE_API_URL` env var (defaults to `http://localhost:8000`).
